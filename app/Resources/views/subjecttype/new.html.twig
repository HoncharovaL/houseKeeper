{% extends 'base.html.twig' %}

{% block body %}
    <h1>Новая дисциплина</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Сохранить" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('subjecttype_index') }}">Возврат к списку</a>
        </li>
    </ul>
{% endblock %}
